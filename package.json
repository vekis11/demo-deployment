{
  "name": "demo-deployment",
  "version": "1.0.0",
  "description": "Simple web app for EC2 deployment via CodePipeline and CodeDeploy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node -e \"require('./server'); process.exit(0);\""
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
